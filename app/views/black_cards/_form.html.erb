<%= form_for(@black_card) do |f| %>
  <% if @black_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@black_card.errors.count, "error") %> prohibited this black_card from being saved:</h2>

      <ul>
      <% @black_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :blanks %><br>
    <%= f.number_field :blanks %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
